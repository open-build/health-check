[supervisord]
nodaemon=true

[program:cron]
directory=/app
command=/bin/bash -c 'crond -f'
autostart=true
autorestart=true
priority=500
stdout_logfile=/supervisord.log
stderr_logfile=/supervisord.log
